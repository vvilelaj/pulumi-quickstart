# pulimi-quickstart repository

Commands
```bash
$ curl -fsSL https://get.pulumi.com | sh
$ mkdir quickstart && cd quickstart && pulumi new aws-typescript
$ pulumi new aws-typescript
$ pulumi up
$ pulumi destroy
```